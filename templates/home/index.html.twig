{% extends 'base.html.twig' %}

{% block title %}Salut!{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Bonjour {{ name }}! ✅</h1>

    Infos utilisateur:
    <ul>
        {% if name is defined %}<li>NOM : {{ name }}</li>{% endif %}
        {% if firstname is defined %}<li>PRENOM: {{ firstname }}</li>{% endif %}
    </ul>
</div>

{% endblock %}
